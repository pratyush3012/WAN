version: '3.8'

services:
  bot:
    build: .
    container_name: discord-gaming-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./bot.db:/app/bot.db
    environment:
      - DATABASE_URL=sqlite:///bot.db
