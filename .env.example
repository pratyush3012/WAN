# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database
DATABASE_URL=sqlite+aiosqlite:///bot.db

# Bot Configuration
OWNER_ID=your_discord_user_id
LOG_LEVEL=INFO

# Web Dashboard Configuration
ENABLE_DASHBOARD=true
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=5000
DASHBOARD_SECRET_KEY=change_this_to_random_secret_key

# Persistent data directory for JSON config files
# On Render: set this to your disk mount path (e.g. /var/data)
# Locally: leave unset (defaults to the bot's directory)
# DATA_DIR=/var/data

# Roblox Integration (Optional - for real game stats)
ROBLOX_API_KEY=your_roblox_open_cloud_api_key_here
ROBLOX_UNIVERSE_ID=your_universe_id_here
ROBLOX_PLACE_ID=your_place_id_here
ROBLOX_WEBHOOK_SECRET=your_webhook_secret_here

# Bloxlink Integration (Optional - for auto-linking Discord <-> Roblox accounts)
# Get your API key from https://blox.link/dashboard/developer
BLOXLINK_API_KEY=your_bloxlink_api_key_here

# Dashboard URL (for /web command)
DASHBOARD_URL=http://localhost:5000

# Discord OAuth2 (for public bot invite page at /invite AND dashboard login)
DISCORD_CLIENT_ID=your_discord_application_client_id
DISCORD_CLIENT_SECRET=your_discord_application_client_secret
# Set this to your deployed URL + /login/discord/callback
# e.g. https://wan-bot.onrender.com/login/discord/callback
DISCORD_REDIRECT_URI=http://localhost:5000/login/discord/callback

# Note: Translation and YouTube features now use FREE services!
# - Translation: Uses free googletrans library (no API key needed)
# - YouTube: Uses free RSS feeds (no API key needed)
